spring:
  application:
    name: monica-lab-homepage

  datasource:
    url: jdbc:mysql://localhost:3306/monica_lab?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: monica
    password: 0109
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true